<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserMapper">
    <select id="logIn" parameterType="com.example.demo.dto.LogInRequestDTO"
            resultType="com.example.demo.model.UserModel">
        SELECT id, first_name AS firstName, last_name AS lastName, color
        FROM users
        WHERE email = #{email}
          AND password = #{password};
    </select>

    <update id="updateUser" parameterType="com.example.demo.dto.UserUpdateRequestDTO">
        UPDATE users
        SET first_name = #{firstName},
            last_name  = #{lastName},
            initial    = #{initial},
            color      = #{color},
            password   = #{password},
            email      = #{email}
        WHERE id = #{id};
    </update>

    <select id="getUser" parameterType="long" resultType="com.example.demo.model.UserModel">
        SELECT id, first_name AS firstName, last_name AS lastName, initial, color, password, email
        FROM users
        WHERE id = #{userId}
    </select>

    <insert id="insertUser" parameterType="com.example.demo.dto.SignupRequestDTO">
        INSERT INTO users (id, first_name, last_name, initial, color, password, email)
        VALUES (#{id}, #{firstName}, #{lastName}, #{initial}, #{color}, #{password}, #{email});
    </insert>

    <select id="userEmailSearchModel" resultType="com.example.demo.model.UserModel">
        SELECT id,
               first_name AS firstName,
               last_name  AS lastName,
               email
        FROM users
        WHERE email LIKE #{likeSearchWord}
        ;
    </select>

</mapper>